1.환경설정
 node 
git hub 에 저장시 주의 사항
1. node_modules 는 올리지 않는다
2.package-lock.json / package.json은 올린다
git add . //update
git status
git commit -m "소스코드 src 폴더로 분류" --commit
git push origin master --마스터에 올리기
git pull origin master --마스트에 내리기

--
git reset HEAD . // .은 모든거 <파일명> 은 해당 파일만